volumes:
  facturation_local_postgres_data: {}
  facturation_local_postgres_data_backups: {}
  

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: facturation_local_django
    container_name: facturation_local_django
    depends_on:
      - postgres
      - mailpit
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: facturation_production_postgres
    container_name: facturation_local_postgres
    volumes:
      - facturation_local_postgres_data:/var/lib/postgresql/data
      - facturation_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  mailpit:
    image: docker.io/axllent/mailpit:latest
    container_name: facturation_local_mailpit
    ports:
      - "8025:8025"
